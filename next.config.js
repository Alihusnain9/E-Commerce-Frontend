/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['icms-image.slatic.net','th.bing.com','english.onlinekhabar.com','th.bing.com'],
      },
}

module.exports = nextConfig
